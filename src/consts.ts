// Source repo for "Edit on GitHub" links — this standalone repo, not the
// architect-learning-lab monorepo notes were extracted from. Content lives at
// `src/content/notes/` directly under this repo's root, so — unlike the
// monorepo's version of this file — there's no subdirectory prefix to add.
export const SOURCE_REPO = 'shipsolid/notes';
export const SOURCE_BRANCH = 'main';
